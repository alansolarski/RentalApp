namespace RentalApp.Views;

/// <summary>Standard About page — just shows app info from the XAML, no ViewModel needed.</summary>
public partial class AboutPage : ContentPage
{
	public AboutPage()
	{
		InitializeComponent();
	}
}
